# Copy this file to .env and fill in real values. Never commit .env.
# Local Docker Postgres (Phase 1 default)
DATABASE_URL=postgresql://finguard:finguard@localhost:5434/finguard

# Neon production Postgres (set in Streamlit Community Cloud secrets, not here)
# DATABASE_URL=postgresql://<user>:<password>@<neon-host>/<db>?sslmode=require

APP_ENV=local
