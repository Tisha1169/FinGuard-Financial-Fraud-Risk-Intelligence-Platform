# Minimum-version bounds (not exact pins) for the heavy ML libraries -
# exact pins were originally verified only against a local bleeding-edge
# Python 3.13/macOS-arm64 setup and some (e.g. pandas 3.0.x) are new
# enough that Streamlit Community Cloud's build image may not have a
# prebuilt wheel yet. Letting pip resolve within these bounds picks
# whatever wheel-available version fits the actual deploy environment.
pandas>=2.2,<3.1
numpy>=1.26,<3.0
scipy>=1.13,<2.0
scikit-learn>=1.4,<2.0
xgboost>=2.0,<4.0
imbalanced-learn>=0.12,<0.15
shap>=0.45,<0.60

# Lighter/stable packages - kept as exact pins, low risk of wheel gaps.
SQLAlchemy==2.0.52
psycopg2-binary==2.9.12
python-dotenv==1.0.1
pydantic==2.13.4
streamlit==1.62.0
plotly==6.9.0
fastapi==0.141.1
uvicorn==0.52.4
httpx==0.28.1
Faker==40.36.0
pyarrow>=14.0,<20.0
pytest==9.1.1
pytest-cov==7.1.0
