# Copy to .streamlit/secrets.toml for local Streamlit runs against Neon,
# or paste into Streamlit Community Cloud's app secrets when deployed.
# Never commit the real .streamlit/secrets.toml (already gitignored).

DATABASE_URL = "postgresql://<user>:<password>@<neon-host>/<db>?sslmode=require"
